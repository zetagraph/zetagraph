import styled from 'styled-components';

const Img = styled.img`
  display: inline-block;
  width: 32px;
  margin: 0 15px;
  vertical-align: bottom;
  transition: 0.5s;

  &:hover {
    opacity: 0.25;
  }
`;

export default Img;
